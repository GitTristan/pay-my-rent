<div class="container">
  <div class="page-header">
    <h1>Users</h1>
  </div>
      <%= link_to "New User", new_user_path, class: "btn btn-primary pull-right" %><br />

      <% @user.each do |u| %>
      <%= user.name %>
      <%= user.email %>
      <% end %>
</div>
